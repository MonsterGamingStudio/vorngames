# For docker-compose.prod.yml (Postgres password used by compose + DATABASE_URL override)
POSTGRES_PASSWORD=change-me-in-production

# PM2 + docker-compose.postgres.yml (порт 5433 на хосте)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5433/vorn_games
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d
STEAM_API_KEY=
STEAM_REALM=http://localhost:3000
STEAM_RETURN_URL=http://localhost:3000/api/auth/steam/callback
FRONTEND_URL=http://localhost:5173
PORT=3000

# Payments (UnitPay + internal API)
PAYMENTS_API_SECRET=change-me-payments-secret
UNITPAY_PROJECT_ID=
UNITPAY_SECRET_KEY=
UNITPAY_PAYMENT_TYPE=card
# Handler URL in UnitPay dashboard: https://www.vorngames.com/api/payments/unitpay/handler
UNITPAY_SKIP_IP_CHECK=true
WS_CALLBACK_URL=https://ws.vorngames.com/api/payment/callback

# Admin & storage
STEAM_ADMIN_IDS=
# local | s3 | r2 (r2 = Cloudflare R2, S3-compatible API)
STORAGE_DRIVER=local
UPLOAD_DIR=./uploads
MAX_UPLOAD_SIZE_MB=100

# Cloudflare R2 + CDN (STORAGE_DRIVER=r2 or s3)
# R2 endpoint: https://<ACCOUNT_ID>.r2.cloudflarestorage.com
# Public images via custom domain in Cloudflare CDN:
# STORAGE_PUBLIC_URL=https://cdn.vorngames.com
S3_ENDPOINT=
S3_BUCKET=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_REGION=auto
STORAGE_PUBLIC_URL=
